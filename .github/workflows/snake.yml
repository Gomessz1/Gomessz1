name: 🐍 Generate Futuristic Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate Futuristic Snake
        uses: Platane/snk@v3
        with:
          github_user_name: Gomessz1
          outputs: |
            dist/github-contribution-grid-snake-futuristic.svg?palette=%23ff00ff,%2300ffff,%23ffea00
            dist/github-contribution-grid-snake-futuristic-dark.svg?palette=%23ff00ff,%2300ffff,%23ffea00&theme=dark
            dist/github-contribution-grid-snake-futuristic-light.svg?palette=%23ff00ff,%2300ffff,%23ffea00&theme=light

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
